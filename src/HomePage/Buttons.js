import React from "react";
export default function ({ handleClick }) {
  return (
    <div>
      <button className="btn btn-primary" value={"17"} onClick={handleClick}>
        NO
      </button>
    </div>
  );
}
